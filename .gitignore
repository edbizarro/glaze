# Dependencies (dev/CI tooling only — the skill itself ships zero-dependency)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*~
.idea/
.vscode/
reports/

# Verification cruft — loose screenshots from browser checks (Render.md says to
# remove these; ignoring them stops a stray capture from being committed).
interceptor-screenshot-*.png
*.screenshot.png